# ─────────────────────────────────────────────────────────────────────────────
# Production redeploy settings — NEVER commit the real file.
#
# Usage:
#   cp deploy.env.example deploy.env
#   Fill in the real values below, then run ./redeploy.sh
#
# deploy.env is listed in .gitignore and will not be staged.
# ─────────────────────────────────────────────────────────────────────────────

APP_CORS_ALLOWED_ORIGINS=https://svirerp.svivanrilski.com
APP_SETTINGS_ENCRYPTION_KEY=
APP_SETTINGS_ENCRYPTION_SALT=
